"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

/*****************************************************************************************************
Account Module, Manages the account profile, creation and editiong of splits as well as the split function.
******************************************************************************************************/

//import {}

var Account = function Account(username, password, email, profile) {
   _classCallCheck(this, Account);

   this.username = username, this.password = password, this.email = user_id, this.profile = profile || {};
}

// createSplit {

// }	    


;

//declares global UserVariable 
//thisAccount =